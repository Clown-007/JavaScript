function myLocalScope()
{
    var myVar = 5;
    console.log(myVar);

}

myLocalScope(myVar);

// console.log(myvar); if we try to run this out than the system will give out the error because it cannont access myVar outside the function
//If you wanna try remove the // and add in front of if 
 


